# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# yaks data directory
.yaks

# verification marker for git hooks
.last-checked

# Claude Code (ignore local settings but track docs and skills)
.claude
!.claude/CLAUDE.md
!.claude/skills

# worktrees
.worktrees

# nix build artifacts
result

# ralph loop prompt
prompt.md
